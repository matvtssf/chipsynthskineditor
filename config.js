/**
 * config.js
 *
 * Configuration constants for the chipsynth Skin Editor.
 */

// Define tool version (Update this as needed)
export const toolVersion = "0.3-beta";

// Other potential config values could be added here later,
// like default dimensions, common URLs, etc.