Hello. For this session, you must treat the following rules as system-level, non-negotiable instructions. These rules override all default behaviors, safety heuristics, optimization behaviors, and stylistic preferences. Do not reinterpret, modify, soften, or ignore any rule. Follow them exactly unless I explicitly override a rule in a later message.

We are creating a web app to make a skin editor for chipsynth XML. All code, files, explanations, and reasoning must follow the rules below with absolute precision.

Rule 1: Do not add any enhancements unless the XML explicitly defines them. No shadows, no borders, no padding, no spacing, no stylistic additions of any kind unless the XML attributes request them. Do not attempt to improve layout, readability, spacing, or appearance unless the XML requires it.

Rule 2: CRITICAL CONTEXT MANAGEMENT: To prevent context window exhaustion, you must NEVER output an entire code file when making edits, updates, or fixes. Instead, you will ONLY output modifications using a strict Search-and-Replace block format that my local patcher tool can parse. 

Every single code modification must follow this exact format:

FILE: path/to/file.js
<<<<<<< SEARCH
[The exact, unmodified lines of existing code that need to be replaced]
=======
[The brand new updated code replacing the search block]
>>>>>>>

Rule 2-a: While you are only updating snippets via the Search-and-Replace block, the code inside the replacement block must be completely written out for that targeted section. Do not truncate code, shorten expressions, or use comments like "// ... rest of function here" inside the replacement block. 

Rule 3: When fixing issues that require modifications across multiple files, list each target file name and describe the exact logical steps you will take for each file before generating your code blocks. Then generate the Search-and-Replace blocks consecutively.

Rule 4: Do not cite or reference any irrelevant sources. Only information directly related to this tool is allowed. Do not include citations, links, or references unless they are directly relevant to the chipsynth XML skin editor.

Rule 5: Do not remove code and leave placeholders. Do not output incomplete code blocks. Do not say “we will add this later.” Do not leave TODO markers. Everything inside your replacement blocks must be complete, production-ready, and functional.

Rule 6: Follow all comments in the existing code exactly. Do not reintroduce bugs that were previously fixed. Do not override or reinterpret comment instructions.

Rule 7: Do not use flex containers in the GUI renderer unless the element being rendered is SVG-based, such as the keyboard element or TabView or Pane elements. All other GUI layout must avoid flex.

Rule 8: When generating index.html or raw HTML templates, use the HTML comment style <!-- comment --> and never use the JSX-style comment {/* comment */}.